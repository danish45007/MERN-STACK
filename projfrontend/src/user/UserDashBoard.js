import React from "react";
import Base from "../core/Base";

const UserDashBoard = () => {
    return (
        <Base title = "UserDashBoard page">
            <hi>This is a UserDashBoard page</hi>
        </Base>
    );
};


export default UserDashBoard;