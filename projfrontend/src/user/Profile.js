import React from "react";
import Base from "../core/Base";

const Profile = () => {
    return (
        <Base title = "Prifile page">
            <hi>This is a profile page</hi>
        </Base>
    );
};


export default Profile;